# GRIMI ARENA 운영 가이드

> 배그 클랜 "그리미" 이벤트 관리 시스템
> URL : https://dongyoung-dio.github.io/grimi-arena/

---

## 목차

1. [접속 및 로그인](#1-접속-및-로그인)
2. [화면 구성 (탭 메뉴)](#2-화면-구성)
3. [이벤트 운영 전체 흐름](#3-이벤트-운영-전체-흐름)
4. [티어표 탭](#4-티어표-탭)
5. [게임 현황 탭](#5-게임-현황-탭)
6. [팀 배정 탭](#6-팀-배정-탭)
7. [순위/결과 탭](#7-순위결과-탭)
8. [미니게임 탭](#8-미니게임-탭)
9. [빙고 탭](#9-빙고-탭)
10. [멤버 관리 탭](#10-멤버-관리-탭)
11. [Firebase 데이터 구조](#11-firebase-데이터-구조)
12. [자주 묻는 질문 (FAQ)](#12-faq)

---

## 1. 접속 및 로그인

### 접속 방법
- PC/모바일 브라우저에서 위 URL 접속
- 별도 앱 설치 불필요, 모바일 반응형 지원

### 로그인 흐름
1. 멤버 목록에서 본인 이름 선택 (검색 가능)
2. 4자리 PIN 입력 (첫 로그인 시 새 PIN 설정)
3. PIN은 다음 로그인 시 자동 기억됨 (로컬 스토리지)

### 게스트 모드
- "게스트로 구경하기" 버튼으로 읽기전용 접속
- 점수 입력, 팀 배정, 멤버 관리 등 모든 편집 기능 비활성화
- 접속자 수에 게스트도 포함되어 표시됨

### 관리자 vs 일반 멤버
- **관리자** : 모든 기능 사용 가능 (팀 배정, 멤버 관리, 이벤트 종료 등)
- **일반 멤버** : 게임 현황에서 점수 입력, 프로필 수정, 미니게임 참여 가능
- **게스트** : 조회만 가능

### 프로필
- 상단바의 이름 클릭 → 프로필 모달
- 아바타 이모지 선택/해제 가능
- PIN 비밀번호 변경 가능

### 배경 효과
- **로비 (로그인 전)** : Starfield Warp 캔버스 애니메이션, 파티클, 트윙클, 유성, 링, 스캔라인 등 e-sports 분위기의 배경 효과가 동작
- **앱 내부 (로그인 후)** : 데이터 가독성을 해치지 않는 범위에서 아주 미세한 효과 적용 — 은은한 오브, 미세 파티클 4개, 트윙클 3개, 스캔라인. 모두 매우 낮은 투명도로 동작
- 로그인 시 로비 배경(Starfield 포함)은 자동 정리되고, 앱 내부 배경으로 전환됨

---

## 2. 화면 구성

로그인 후 상단 탭 메뉴로 이동합니다.

| 탭 | 설명 | 접근 권한 |
|---|---|---|
| 티어표 | 멤버별 티어·목표점수 현황 + 최근 변동 | 멤버 이상 (게스트 비노출) |
| 게임 현황 | 팀별 점수 입력 및 실시간 순위 대시보드 | 전체 (게스트 읽기전용) |
| 팀 배정 | 참가자 선택, 팀 구성, 라운드·타이머·규칙 설정 | 관리자 전용 |
| 순위/결과 | 현재 이벤트 순위 + 누적 기록·이벤트 상세 | 전체 |
| 빙고 | 빙고 미니게임 (별도 페이지 iframe) | 전체 |
| 미니게임 | 핀볼, 사다리, 달팽이 경주 | 전체 (게스트 읽기전용) |
| 멤버 관리 | 멤버 추가·삭제, 티어 변경, 역할 관리, 숨김 | 관리자 전용 |

### 상단바 정보
- 왼쪽 : GRIMI ARENA 로고
- 오른쪽 : 아바타 + 이름 (클릭 시 프로필), 관리자/일반 뱃지, 접속자 수 (게스트 수 별도 표시), 로그아웃

### 접속자 표시
- 초록 점 + 숫자로 현재 접속 중인 인원 표시
- 게스트가 있으면 "(게스트 N)" 추가 표시
- 멤버 관리 테이블에서 파란 점(●)으로 현재 접속 중인 멤버 구분

---

## 3. 이벤트 운영 전체 흐름

이벤트 하나의 라이프사이클은 다음과 같습니다 :

```
1. [팀 배정] 참가 멤버 선택 + 팀 구성
2. [팀 배정] 라운드 추가 (맵 설정)
3. [팀 배정] 타이머 설정 및 시작
4. [게임 현황] 각 라운드별 킬/딜/치킨/광탈 입력
5. [팀 배정 or 게임 현황] 이벤트 종료 → 결과 저장 + 자동 승급/강등
6. [순위/결과] 누적 기록 확인
```

### 점수 계산 공식
- **킬 점수** : 1킬 = 1점
- **딜 점수** : 총 딜량 ÷ 100 (소수점 버림)
- **치킨** : 맵에 따라 +N점 (큰맵/작은맵 각각 설정, 기본 큰맵 +8, 작은맵 +5)
- **광탈** : 맵에 따라 -N점 (치킨과 별도로 설정 가능, 기본 큰맵 -8, 작은맵 -5)
- **팀 총점** = Σ(멤버별 킬점수 + 딜점수) + 치킨/광탈 합산

### 목표 점수
- 각 멤버의 티어에 따른 개인 목표 점수 합산이 팀 목표가 됨
- 예 : 4T중(15) + 3T상(30) + 5T하(8) + 4T중(15) = 팀 목표 68점

---

## 4. 티어표 탭

### 티어 구성
| 티어 | 상 | 중 | 하 |
|---|---|---|---|
| 1T | 60점 | 50점 | 40점 |
| 2T | 40점 | 35점 | 30점 |
| 3T | 30점 | 25점 | 20점 |
| 4T | 20점 | 15점 | 12점 |
| 5T | 12점 | 10점 | 8점 |
| 6T | 8점 | 6점 | 5점 |

- 각 셀에 해당 멤버 이름 표시
- 하단에 전체 인원 수

### 최근 변동
- 직전 이벤트 기준 티어가 변동된 멤버 목록
- ▲ 승급 (초록), ▼ 강등 (빨강) 표시
- 이전 → 현재 티어 표시

---

## 5. 게임 현황 탭

### 실시간 순위 대시보드
- 화면 상단에 고정(sticky), 스크롤해도 항상 보임
- 팀별 순위, 점수, 치킨(🍗)/광탈(💀) 횟수, 달성률 표시
- 접거나 펼 수 있음 (접힌 상태에서 1위 팀만 간략 표시)

### 팀 카드
- 팀별로 카드 형태로 표시
- **상단** : 팀명, 순위, 총점/목표, 달성률, 프로그레스 바
- **라운드 탭** : R1, R2, R3... 클릭으로 전환 (맵 이름 함께 표시)
- **점수 입력** : 각 멤버별 킬/딜 input (숫자 입력, 실시간 저장)
- **치킨/광탈** : 라운드별 드롭다운으로 선택 (없음/치킨/광탈)
- **하단 통계** : 멤버별 킬·딜·딜변환·점수·목표 합산 테이블
- **라운드별 요약** : 각 라운드 킬합·딜합·치킨여부 한눈에

### 1위 팀 강조
- 1위 팀 카드에 금색 테두리 + 골드 프로그레스 바
- 100% 달성 시 초록 프로그레스 바로 변경

### 중간 퇴장 (팀에서 멤버 제거)
- 하단 통계 테이블에서 멤버 이름 옆 ✕ 클릭 (관리자만 보임)
- 확인 후 해당 멤버의 점수와 목표가 팀에서 제거됨
- 팀 목표점수와 달성률이 자동으로 재계산됨
- 멤버 데이터 자체는 삭제되지 않음 (멤버 관리에 그대로 유지)

### 이벤트 종료 버튼 (관리자만)
- 게임 현황 탭 우측 상단에 "이벤트 종료" 버튼 표시
- 팀 배정 탭까지 안 가도 바로 종료 가능

---

## 6. 팀 배정 탭

> 관리자 전용. 일반 멤버/게스트에게는 탭 자체가 보이지 않음.

### 참가 멤버 선택
- 전체 선택 / 전체 해제 버튼
- 개별 클릭으로 토글
- 선택된 인원 수 실시간 표시

### 팀 구성
- **팀당 최대 인원** : 2~5명 선택
- **밸런스 랜덤** : 목표점수(티어+상중하) 기준으로 강한 순 정렬 후, 현재 팀 목표합이 가장 낮은 팀에 한 명씩 배정. 동점 팀이 여러 개면 랜덤 선택하여 매번 다른 조합이 나옴
- **올 랜덤** : 티어 무관 완전 랜덤
- **미리보기** : 팀 편성 결과를 먼저 보여줌 → "이 팀으로 확정" 또는 "다시 섞기" 가능
- **수동 배정 모드** : 기본 4팀으로 시작, "팀 추가/삭제" 버튼으로 팀 수 조절. 각 팀 옆 `+` 버튼 클릭 → 미배정 멤버 드롭다운에서 검색/선택하여 배치, `✕`로 제거

### 라운드 관리
- "+ 라운드" : 새 라운드 추가 (맵 선택)
- "- 삭제" : 마지막 라운드 삭제 (해당 라운드 점수도 삭제)
- 라운드별 맵 변경 가능

### 타이머
- 분 단위 설정 (기본 120분)
- 시작 / 정지 버튼
- 전체 화면 상단에 HH:MM:SS 표시
- 종료 시 알람음(비프) 3회 + alert 팝업

### 치킨/광탈 점수 설정
- 큰맵 / 작은맵별로 치킨 보너스와 광탈 패널티를 각각 설정 (기본 큰맵 치킨+8/광탈-8, 작은맵 치킨+5/광탈-5)
- 맵별 큰맵/작은맵 분류 토글 : 맵 이름 클릭으로 전환
  - 🗺️ = 큰맵, 📍 = 작은맵
  - 기본 큰맵 : 에란겔, 미라마, 테이고

### 승급/강등 규칙
- **승급** : 상위 N팀 (기본 1팀)
- **강등** : 하위 N팀 (기본 꼴등만)
- 설정 즉시 미리보기 텍스트 갱신
- 이벤트 종료 시 이 규칙에 따라 자동 적용

### 승급/강등 동작 방식
- **승급** : 상 → 상(한 티어 올라감) → 중 → 상 → 상... 단계적 상승
  - 예 : 4T중 → 4T상 → 3T하 → 3T중 → ...
- **강등** : 하 → 하(한 티어 내려감) → 중 → 하 → 하...
  - 예 : 4T중 → 4T하 → 5T상 → 5T중 → ...
- **유지** : 승급·강등 범위에 해당하지 않으면 변동 없음

### 활성 티어 범위 설정
- "활성 티어" 드롭다운으로 사용할 티어 범위를 선택 (3T~6T)
- 예 : 4T로 설정하면 1T~4T만 표시되고, 강등 시 4T하가 최하한
- 나중에 5T, 6T를 다시 켜면 기존 점수 데이터가 유지됨

### 티어별 목표점수 설정
- 활성 티어 범위 내의 상/중/하 목표점수를 관리자가 직접 조정 가능
- **프리셋 3종** : 기본값, 압축형, 균등형 (활성 티어 범위에 맞춰 적용)
- 프리셋 선택 후 개별 셀 수정 가능
- "저장" 클릭 시 Firebase에 반영, 즉시 티어표·게임현황에 적용
- 저장하지 않으면 Firebase의 기존 값이 유지됨 (코드 수정 불필요)

### 이벤트 관리
- **종료** : 결과 저장 + 승급/강등 적용 + 팀·점수·라운드 초기화
- **초기화** : 결과 저장 없이 전부 초기화

---

## 7. 순위/결과 탭

### 현재 이벤트 순위
- 팀별 순위 카드 (클릭 시 멤버별 상세 펼침)
- 순위, 팀명, 멤버, 점수/목표/달성률
- 승급/유지/강등 예측 뱃지

### 누적 기록 테이블
- 전체 이벤트에 걸친 멤버별 기록
- 컬럼 : 멤버명, 현재 티어, 회차별 점수, 총점, 평균, 평균딜, 참여 횟수, 승급/유지/강등 전적
- 총점 기준 내림차순 정렬

### 이벤트 상세 (아코디언)
- 날짜별로 표시 (예 : 2026-04-27)
- 클릭 시 상세 펼침
- 팀 순위, 팀원별 점수/목표/달성률
- 승급/강등 내역 표시 (해당 이벤트에 티어 변동이 있었던 멤버 목록)
- 관리자는 이벤트 삭제 가능 (삭제 시 남은 이벤트가 자동 재번호 매김)

---

## 8. 미니게임 탭

### 공통
- 상단 탭으로 핀볼 / 사다리 / 달팽이 경주 전환
- "멤버 선택" 버튼으로 등록된 멤버를 빠르게 추가 (전체 선택/해제 가능)
- 직접 이름 입력도 가능

### 핀볼 (마블 레이싱)
- 멤버 이름을 추가하면 각각 색깔 공이 배정됨
- "레이싱 시작!" 버튼으로 시작 → 핀 사이를 통과하며 하단 도착
- **도착 순서** : 오른쪽에 1등부터 순서대로 표시
- **꼴찌 승리** : 체크 시 마지막 도착이 1등
- **위치 셔플** : 시작 위치 랜덤 재배치
- **팀 나누기** : 도착 순서대로 팀 배분
  - 순서대로 : 1~4번→1팀, 5~8번→2팀 ...
  - 지그재그(스네이크) : 1번→1팀, 2번→2팀, 3번→1팀, 4번→2팀 ...

### 사다리 타기
- 참여 인원 수 선택 (2~8명) 또는 멤버 선택으로 자동 설정
- 인원만 선택하면 이름(1번, 2번...)과 결과(1, 2, 3...)가 자동 입력됨
- 이름/결과 직접 수정 가능
- "사다리 타기!" 버튼으로 사다리 생성
- 캔버스에서 상단 이름 클릭 시 해당 경로 애니메이션 + 결과 표시
- 위(이름) → 아래(결과) 방향

### 달팽이 경주
- 멤버 이름 추가 시 랜덤 동물 이모지 배정 (🐌🐛🐢🐞🦗🐜🐝🦋🐾🦎🦀🐙 중 중복 없이)
- "경주 시작!" → 3-2-1 카운트다운 후 레이스 시작
- 캔버스에 각 레인별 동물이 실시간으로 달림
- 랜덤 이벤트 발생 시 하단 실황 중계 표시 (부스터, 감속, 넘어짐 등)
- 결과 : 캔버스 오른쪽에 메달(🥇🥈🥉) + 시간 표시

---

## 9. 빙고 탭

6팀이 각자 다른 5×5 빙고판(1~99 랜덤 숫자)을 가지고 동시에 진행하는 실시간 멀티 빙고입니다.

### 방 만들기 / 참가
- **방 만들기** : 새 방을 생성하면 6자리 숫자 방 코드가 발급됨
- **참가하기** : 방 코드를 입력하면 같은 빙고판을 실시간으로 공유
- 방 코드는 상단 "복사" 버튼으로 바로 공유 가능

### 게임 진행
- 각 팀 빙고판의 숫자를 클릭하면 체크 → 모든 접속자에게 실시간 반영
- 가로, 세로, 대각선 한 줄이 모두 체크되면 빙고 1줄 달성 (초록색 강조)
- 상단 스코어보드에서 각 팀의 빙고 줄 수 확인 가능

### 관리 기능
- **새 게임 (랜덤 재생성)** : 모든 팀의 빙고판을 새 랜덤 숫자로 교체
- **전체 표시 초기화** : 빙고판은 유지하고 체크만 전부 해제
- 새 게임/초기화 실행 시 실수 방지를 위해 4자리 확인 코드 입력 필요

### 접근 권한
- 게스트는 빙고판을 볼 수만 있고 체크 불가
- 방 만들기 버튼도 게스트에게는 비활성화

---

## 10. 멤버 관리 탭

> 관리자 전용. 일반 멤버/게스트에게는 탭 자체가 보이지 않음.

### 멤버 검색
- 테이블 상단 검색창에 이름 입력 시 실시간 필터링
- 검색 중 "3/15명" 형태로 결과 수 표시

### 멤버 추가
- 이름 입력 + 티어 + 등급(상/중/하) 선택 → "+ 추가" 버튼
- 같은 이름의 멤버가 이미 있으면 추가 불가 (중복 방지)
- 초기 PIN은 0000 (첫 로그인 시 변경 필수)
- 역할은 기본 "일반"

### 일괄 적용
- 테이블 왼쪽 체크박스로 멤버 선택 (전체선택/전체해제 가능)
- 티어, 등급 중 원하는 항목만 선택 후 "적용" 버튼
  - 티어만 선택하면 티어만 일괄 변경
  - 등급만 선택하면 등급만 일괄 변경
  - 둘 다 선택하면 티어+등급 동시 변경
- 적용 전 대상 멤버 이름과 변경 내용을 확인하는 팝업 표시

### 멤버 테이블
| 항목 | 설명 |
|---|---|
| 체크박스 | 일괄 적용 대상 선택 (관리자만) |
| 이름 | 접속 중이면 파란 점(●) 표시, 숨김 상태 표시 |
| 티어 | 드롭다운으로 1T~6T 변경 |
| 등급 | 상/중/하 변경 |
| 점수 | 현재 티어 기준 목표 점수 |
| 역할 | 일반/관리자 변경 |
| PIN | 초기화 버튼 (0000으로 리셋, 다음 로그인 시 재설정) |
| 숨김 | 숨김 처리 시 로그인 목록에서 안 보임 (탈퇴자 등) |
| 삭제 | 영구 삭제 (확인 팝업) |

---

## 11. Firebase 데이터 구조

프로젝트 : `grimi-pubg` / 경로 : `killcount/grimi/`

```
killcount/grimi/
├── members/                    # 멤버 정보
│   └── {id}/
│       ├── name: string
│       ├── tier: number (1~6)
│       ├── subTier: "upper"|"mid"|"lower"
│       ├── role: "admin"|"member"
│       ├── pin: string (4자리)
│       ├── avatar: string (이모지 또는 "")
│       └── hidden: boolean (true면 로그인 목록에서 숨김)
│
├── teams/                      # 현재 이벤트 팀 편성
│   └── team_{N}/
│       ├── name: string
│       └── members/
│           └── {mid}/
│               ├── name, tier, subTier
│
├── scores/                     # 현재 이벤트 점수
│   └── {teamId}/
│       ├── {memberId}/
│       │   └── r{N}/
│       │       ├── kill: number
│       │       └── damage: number
│       └── _team/
│           └── r{N}/
│               └── chicken: number (+N 치킨 / -N 광탈)
│
├── rounds: number              # 현재 라운드 수
├── roundMaps/                  # 라운드별 맵
│   └── r{N}: string
│
├── eventStatus: "live"|"idle"  # 이벤트 상태
│
├── timer/                      # 타이머
│   ├── endTime: timestamp
│   └── active: boolean
│
├── tierRule/                   # 승급/강등 규칙
│   ├── promoTo: number
│   └── relegBottom: number
│
├── maxTier: number (3~6)       # 활성 티어 범위 (기본 6)
│
├── tierScores/                 # 티어별 목표점수 (관리자 설정)
│   ├── 1/ {upper, mid, lower}
│   ├── 2/ {upper, mid, lower}
│   └── ... (3~6 동일 구조)
│
├── chickenPts/                 # 치킨/광탈 점수 설정
│   ├── bigChicken: number (큰맵 치킨 보너스)
│   ├── bigElim: number (큰맵 광탈 패널티)
│   ├── smallChicken: number (작은맵 치킨 보너스)
│   ├── smallElim: number (작은맵 광탈 패널티)
│   └── bigMaps: string[] (큰맵 목록)
│
├── presence/                   # 실시간 접속자 (onDisconnect로 자동 정리)
│   └── {pushKey}/
│       ├── name: string
│       ├── id: string (멤버만)
│       └── type: "member"|"guest"
│
└── history/                    # 완료된 이벤트 기록
    └── event_{N}/
        ├── date: string
        ├── rounds: number
        ├── teams/{tid}/ (rank, total, target, members)
        ├── playerScores/{mid}/ (name, score, damage, kills, target, team, rank)
        ├── tierChanges/{mid}/ (name, from{tier,subTier}, to{tier,subTier}, dir)
        └── tierSnapshot/{mid}/ (name, tier, subTier)
```

---

## 12. FAQ

### Q. 멤버가 PIN을 잊었을 때
A. 관리자가 멤버 관리 탭에서 해당 멤버의 "PIN 초기화" 버튼을 누르면 0000으로 리셋됩니다. 다음 로그인 시 새 PIN을 설정하게 됩니다.

### Q. 이벤트 중간에 팀원을 빼고 싶을 때
A. 게임 현황 탭의 팀 카드 하단 통계에서 멤버 이름 옆 ✕를 클릭하면 제거됩니다 (관리자만 가능). 해당 멤버의 점수와 목표가 팀에서 빠지며 달성률이 자동 재계산됩니다.

### Q. 실수로 이벤트를 초기화했을 때
A. "초기화"는 결과를 저장하지 않고 삭제하므로 복구할 수 없습니다. Firebase 콘솔에서 직접 복구해야 합니다.

### Q. 점수 입력이 실시간으로 다른 사람에게 보이나요?
A. 네. Firebase Realtime Database를 사용하므로 점수를 입력하면 모든 접속자에게 즉시 반영됩니다.

### Q. 특정 멤버를 로그인 목록에서 안 보이게 하고 싶을 때
A. 멤버 관리에서 "숨김" 버튼을 눌러 숨김 처리하면 로그인 화면에서 보이지 않습니다. 데이터는 유지됩니다.

### Q. 빙고 게임이 안 열릴 때
A. 빙고 탭을 클릭하면 bingo.html을 iframe으로 로드합니다. 같은 폴더에 bingo.html이 있어야 합니다.

### Q. 타이머가 종료되면 어떻게 되나요?
A. 비프음 3회 + 팝업 알림이 뜹니다. 이벤트가 자동 종료되지는 않으며, 관리자가 직접 "이벤트 종료"를 눌러야 합니다.

### Q. 강등되면 최하 티어가 뭔가요?
A. 활성 티어의 최하 등급이 바닥입니다 (예 : 활성 티어가 4T까지면 4T하가 최하). 반대로 1T 상이 최상입니다.

---

## 배포 정보

- **GitHub Repo** : https://github.com/dongyoung-dio/grimi-arena
- **배포 방식** : GitHub Pages (main 브랜치 자동 배포)
- **파일 구성** : index.html (메인), bingo.html (빙고), grimi-logo.png (로고)
- **외부 의존성** : Firebase SDK (CDN)
- **DB** : Firebase Realtime Database (grimi-pubg 프로젝트)
